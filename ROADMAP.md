# MiniClaw Roadmap

> 从模型到 Agent 的进化路线

---

## 设计哲学

MiniClaw 不是另一个 ChatBot，而是一个**有记忆、有性格、能自主行动的 Agent**。

核心公式：**Agent = 模型 + 记忆 + 工具 + 规划 + 纠错 + 自主性**

---

## 版本路线

### v0.5.x — 打磨期（当前）

**目标**：修复已知 Bug，夯实基础

| # | 问题 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | 域名实体不识别 | P0 | ⏳ |
| 2 | 闲聊式信息不产生记忆 | P1 | ⏳ |
| 3 | 中文关键词提取弱（缺 jieba） | P1 | ⏳ |
| 6 | `min_importity` 拼写错误 | P0 | ⏳ |
| 8 | 去重逻辑太严格 | P2 | ⏳ |
| 9 | 纯数字 ID 不提取 | P2 | ⏳ |
| 10 | 版本号不一致 | P0 | ⏳ |
| 11 | HttpFetchSkill 版本号过时 | P2 | ⏳ |

**交付标准**：所有 P0 Bug 清零，P1 有方案，代码通过自测

---

### v0.6.0 — 规划执行引擎

**目标**：从"一问一答"到"目标拆解+多步执行"

**新增模块**：
- `plan.py`：目标拆解（Goal → Steps）
- `executor.py`：步骤执行 + 纠错循环
- `replanner.py`：动态重规划（失败后调整，不是从头来）

**核心能力**：
```
用户："帮我部署这个服务"
Agent：
  Step 1: 检查配置 → ✅
  Step 2: 拉取镜像 → ✅
  Step 3: 启动容器 → ❌ 超时
  Reflect: 可能是网络问题
  Replan: Step 3 → 用本地镜像
  Step 3: 启动容器 → ✅
  Step 4: 验证健康 → ✅
  Step 5: 通知完成 → ✅
```

**Agent 层次**：第2层(100%) → 第3层(70%)

---

### v0.7.0 — 性格系统 + 情绪状态

**目标**：性格从经历中生长，不是写死的配置

**新增模块**：
- `personality.py`：五维性格滑块（主动性/话量/谨慎度/正式度/幽默感）
- `emotional_state.py`：情绪状态（认知负荷/连续出错/空闲时长）
- `personality_state` 表：性格持久化 + 变化事件追踪

**核心机制**：
- 性格从互动中演变（用户频繁打断→话量降低，犯错被纠正→更谨慎）
- 情绪影响行为（连续出错→强制深思，负荷高→更简洁）
- 性格影响 System Prompt 生成（动态注入）

**Agent 层次**：外在表现有了"人味"

---

### v0.8.0 — 费曼验证 + 元认知

**目标**：知道自己知道什么、不知道什么

**新增模块**：
- `feynman.py`：费曼验证机制（新规则自检）
- `metacognition.py`：元认知（能力画像+边界感知）
- `meta` 表：能力领域 + 置信度

**核心机制**：
- 新产生的规则不直接信任，先在低优先级试用
- 验证方式：自然验证（被使用且未纠正）、主动验证（模型自检）
- 元认知：每个领域有 confidence，知道擅长什么/不擅长什么
- 不确定时主动说"我不确定，建议确认"

---

### v0.9.0 — 自主运行

**目标**：没人找时，Agent 也在运转

**新增模块**：
- `autonomous.py`：自主运行循环
- `scheduler.py`：定时任务调度
- `watcher.py`：环境感知（文件变化/日志/指标）

**核心机制**：
- 定时巡检 → 发现异常 → 主动修复 → 通知
- 空闲时 → 整理记忆 → 巩固经验 → 优化规则
- 监控变化 → 判断是否需要行动 → 主动行动

**Agent 层次**：第3层(100%) → 第4层(60%)

---

### v1.0.0 — 关系系统 + 协作

**目标**：与用户建立关系，支持多 Agent 协作

**新增模块**：
- `relationship.py`：用户关系管理（陌生→熟悉→信任→默契）
- `expression.py`：三维联合表达（性格×关系×情绪）
- `social.py`：社交上下文（群聊/私聊切换、记忆隔离）
- `growth.py`：成长轨迹（可回溯的演变记录）

**Agent 层次**：第4层(100%) → 第5层(起步)

---

## 版本量化目标

| 版本 | Agent 层次 | 代码行数 | 模块数 | Bug 数 | 核心里程碑 |
|------|-----------|---------|--------|--------|-----------|
| v0.5.0 | L2(100%) | 2,075 | 9 | 8 | Tool Use 完成 |
| v0.5.1 | L2(100%) | ~2,200 | 9 | ≤3 | Bug 清零 |
| v0.6.0 | L3(70%) | ~3,500 | 12 | ≤2 | 规划执行引擎 |
| v0.7.0 | L3(80%) | ~4,500 | 15 | ≤2 | 性格系统 |
| v0.8.0 | L3(90%) | ~5,200 | 17 | ≤1 | 元认知 |
| v0.9.0 | L4(60%) | ~6,000 | 20 | ≤1 | 自主运行 |
| v1.0.0 | L4(100%) | ~7,500 | 24 | 0 | 关系+协作 |

---

## 设计文档参考

完整设计见 `docs/memory-system-design.md`（25章，覆盖记忆、性格、情绪、关系、表达、社交、成长）
